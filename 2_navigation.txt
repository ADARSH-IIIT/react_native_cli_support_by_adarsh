download packages of react native navigation native stack dependencies , bottom tab bar screen safe area screen etc etc 








import React, { useState } from 'react';
import { View, Text, Button, TextInput } from 'react-native';
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const Stack = createNativeStackNavigator();
const Tab = createBottomTabNavigator();
const DetailStack = createNativeStackNavigator(); // New stack for nested navigation

const HomeScreen = ({ navigation }) => {
  const [input, setInput] = useState('');

  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Home Screen</Text>
      <TextInput
        style={{
          height: 40,
          borderColor: 'gray',
          borderWidth: 1,
          width: 200,
          marginVertical: 10,
          paddingHorizontal: 10,
        }}
        placeholder="Enter something..."
        value={input}
        onChangeText={setInput}
      />
      <Button title="Go to Details" onPress={() => navigation.push('Detail')} />
    </View>
  );
};

const Tab1 = ({ navigation }) => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Tab 1</Text>
    <Button title="Go to Home" onPress={() => navigation.goBack()} />
  </View>
);

const Tab2 = ({ navigation }) => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Tab 2</Text>
    <Button title="Go to Project" onPress={() => navigation.navigate('Project')} />
  </View>
);

const Tab3 = ( {navigation }) => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Tab 3</Text>
    <Button title="Go back" onPress={() => navigation.goBack()} />

  </View>
);

const ProjectScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>This is the Project Page</Text>
  </View>
);

// Create a new stack inside DetailScreen
const DetailStackScreen = () => (
  <DetailStack.Navigator screenOptions={{ headerShown: false }}>
    <DetailStack.Screen name="Tabs" component={DetailScreenTabs} />
    <DetailStack.Screen name="Project" component={ProjectScreen} />
  </DetailStack.Navigator>
);

// Tabs inside the Detail screen
const DetailScreenTabs = () => (
  <Tab.Navigator screenOptions={{ headerShown: false }}>
    <Tab.Screen name="Tab1" component={Tab1} />
    <Tab.Screen name="Tab2" component={Tab2} />
    <Tab.Screen name="Tab3" component={Tab3} />
  </Tab.Navigator>
);

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator screenOptions={{ headerShown: false, animation: 'slide_from_left' }}>
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Detail" component={DetailStackScreen} /> 
      </Stack.Navigator>
    </NavigationContainer>
  );
}
